## What and why

<!-- What changes, and which problem it solves. Link the issue: "Closes #123". -->

## Checklist

- [ ] `uv run pytest` and `uv run ruff check .` pass
- [ ] A test covers new behavior that a plausible bug could break
- [ ] `CHANGELOG.md` has a line under `Unreleased` for a user-visible change
- [ ] The README image is regenerated if the pane looks different (`uv run python scripts/screenshot.py`)
